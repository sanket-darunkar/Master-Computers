[build]
  command         = "npm run build"
  publish         = "dist"

# SPA fallback — all routes serve index.html so the React router
# handles /certificate-verification, /admin/login, /admin/dashboard, etc.
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
